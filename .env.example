# --- Shared ---
# Postgres connection string used by API + enricher + migrations.
DATABASE_URL=postgresql+psycopg://app:app@localhost:5432/supportintel
# Kafka bootstrap address.
BOOTSTRAP=localhost:9092

# --- API service ---
# Kafka topic for incoming tickets produced by the API.
TOPIC_IN=support.tickets.v1

# --- Enricher service ---
# Kafka topic to consume tickets from.
ENRICHER_TOPIC_IN=support.tickets.v1
# Kafka topic for enriched output.
TOPIC_OUT=support.enriched.v1
# Kafka topic for dead-letter queue.
TOPIC_DLQ=support.dlq.v1
# Kafka consumer group for enricher.
GROUP_ID=support-enricher
# Anthropic model name.
MODEL=claude-sonnet-4-5-20250929
# Anthropic API key (required unless STUB_LLM=1).
ANTHROPIC_API_KEY=your-anthropic-api-key
# Embedding model name (SentenceTransformers).
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# Cross-encoder reranker model name.
RERANK_MODEL=BAAI/bge-reranker-base
# Final number of KB chunks to use in prompt.
KB_TOP_K=5
# Number of candidates to retrieve before reranking.
KB_CANDIDATES=20
# Enable reranking (true/false).
RERANK_ENABLED=true
# Use stubbed LLM responses instead of Anthropic (1/0).
STUB_LLM=0
